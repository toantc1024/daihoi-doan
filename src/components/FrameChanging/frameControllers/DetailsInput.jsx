import React from "react";

// Components

const Inputs = ({ uploadedImage, setUploadedImage, setHeight, setWidth }) => (
  <div>
    <label htmlFor="contained-button-file">UPLOAD </label>
   
  </div>
);

export default Inputs;
